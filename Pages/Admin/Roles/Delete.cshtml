@page "/admin/roles/delete/{id}"
@model WebAppTest.Admin.Pages_Roles.DeleteModel
@{
    ViewData["Title"] = "Delete Role";
    Layout = "_LayoutAdmin";
}

<h1>Delete Role</h1>

<h4>Bạn có chắc muốn xóa role này không ?</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <span class="text-danger">*</span>
                <label asp-for="Input.RoleName" class="control-label"></label>
                <input asp-for="Input.RoleName" class="form-control" disabled />
                <span asp-validation-for="Input.RoleName" class="text-danger"></span>
            </div>
            <div class="form-group mt-2">
                <input type="submit" value="Delete" class="btn btn-danger" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}